var React = require('react')
;

var SideMenu= React.createClass({
    render: function () {
        return (
            <div className = "side-menu">
                This is Side Menu
            </div>
        )
    }
});

module.exports = SideMenu;